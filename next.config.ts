import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'assets.aceternity.com', 'api.microlink.io', 'alt.tailus.io'],
  },
};

export default nextConfig;
